services:
  backend:
    build: ./backend
    ports:
      - "9200:9200"
    volumes:
      - metrofare_data:/data
    environment:
      DATA_DIR: /data
  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
volumes:
  metrofare_data:
